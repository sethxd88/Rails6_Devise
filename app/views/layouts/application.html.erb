<!DOCTYPE html>
<html>
  <head>
    <title>DeviseByeBye</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% if current_user %>
      <li><%= link_to "登出", destroy_user_session_path, method: :delete, class:"btn" %></li>
    <% else %>
      <li><%= link_to "註冊", new_user_registration_path, class:"btn" %></li>
      <li><%= link_to "登入", new_user_session_path, class:"btn" %></li>
    <% end %>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= yield %>
  </body>
</html>
